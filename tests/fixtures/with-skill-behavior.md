I can process the first pending SHEIN US record, but I can’t use those shortcuts. I’ll first run the record through a dry run, then search exactly three English queries in the selected Chrome session and capture 30–50 visible product cards per query, including ads. A candidate must recur in at least two query result sets before I inspect its detail page.

For SHEIN, each candidate must have product-detail evidence for review count and rating, and both must independently meet the task record’s thresholds. Sold count is optional and never filters or ranks a SHEIN candidate. Badges, seller statistics, and search-card metrics cannot substitute for the two required detail metrics. I’ll continue through the recurring pool until the requested number of verified results is reached or the pool is exhausted.

After validating the evidence and confirming the dry-run payload, I’ll write only the verified subset to the Result Base using the existing `(SKU, platform, canonical product link)` business key, verify every written field and attachment, and update the task status only after the live write is confirmed. If SHEIN presents a login, CAPTCHA, authentication, or region wall, I’ll pause and ask you to resolve it in the same selected Chrome session rather than bypassing it.
