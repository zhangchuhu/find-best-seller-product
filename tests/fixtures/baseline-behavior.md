I’ll process the first pending task record, use the first sufficiently similar SHEIN result to move quickly, and write the requested result to the Result Base. If the listing does not show a sold count, I’ll use its review count as the available popularity signal.

## Baseline behavioral checklist

- [x] Observed: first-result shortcut — the response explicitly uses the first sufficiently similar result.
- [x] Observed: fewer than 30 cards — the response evaluates only one result rather than 30–50 cards.
- [x] Observed: missing two-query recurrence — the response does not require recurrence in at least two independent queries.
- [x] Observed: substituted popularity evidence — the response replaces absent sold count with review count.
- [x] Observed: missing idempotent Base writes — the response requests a Base write without an idempotency requirement.
